.message
  .avatar
    = gravatar_for message.user
  .message-information
    .author
      = message.user.name
    .timestamp
      = message.hours
    - if message.mentioned?
      .message-body.mentioned
        = message.body
    - else
      .message-body
        = message.body
